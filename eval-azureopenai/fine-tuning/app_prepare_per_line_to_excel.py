# this is generated by chatgpt 4
import json
import pandas as pd

# Change these paths to match your input and output files
input_file_path = "c:/tmp/1.txt"
output_file_path = "c:/tmp/output.xlsx"

# Create an empty list to hold the formatted lines
output_lines = []

# Open the input file and iterate over each line
with open(input_file_path, "r", encoding='utf-8') as input_file:
    for line in input_file:
        # Skip the line if it's empty or starts with "Image"
        if not line.strip() or line.startswith("Image"):
            continue
        
        # Append the formatted line to the output_lines list
        output_line = {"prompt": "", "completion": line.strip()}
        output_lines.append(output_line)

# Convert the list of dictionaries to a Pandas DataFrame
output_df = pd.DataFrame(output_lines)

# Write the DataFrame to an Excel file
output_df.to_excel(output_file_path, index=False)